li.log.web-outgoing(class= done ? 'success' : 'failure')
	span.date= date
	span.course outgoing
	span.server WEB
	span.remote-addr= remoteAddr
	span.protocol= protocol
	span.method= method
	span.status-code= statusCode
	span.path= path