article
	header
		h2
			span.number= post.threadCursor
			a(href='/' + post.user.screenName)= post.user.name
			time.absolute(datetime= post.createdAt)= post.displayCreatedAt
			time.relative(datetime= post.createdAt, data-display-type='relative')
	div.content
		p.text= post.text