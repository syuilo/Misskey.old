li.log.api-incoming(class= done ? 'success' : 'failure')
	span.date= date
	span.course incoming
	span.server API
	span.remote-addr(style= 'background:' + color)= remoteAddr
	span.protocol= protocol
	span.method= method
	span.path= path
	span.ua= ua