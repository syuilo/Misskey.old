doctype html

html(lang='ja', dir='ltr')
	head
		include ../common/head
		title スレッドの設定
		link(rel='stylesheet', href='/resources/styles/bbs-thread-settings.css')
		script(src='/resources/scripts/bbs-thread-settings.js')
	
	body
		include ../common/header
		
		main(role='main')
			form#form
				h1
					i.fa.fa-pencil-square-o
					| スレッドの設定
				div.main
					label
						h2 タイトル
						input.ui-input-text(type='text', name='title', placeholder=thread.title, required)= thread.title
					label
						h2 アイキャッチ
						div.icon
							p 画像をドロップまたは選択
							img.image(src=url + '/img/bbs-thread-eyecatch/' + thread.id, alt='eyecatch')
							input(name='image', type='file', accept='image/*')
					input.submit-button.ui-button(type='submit', value!='更新 &#xf1d8;')
