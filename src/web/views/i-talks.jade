extends ./layouts/home

block title
	title Talks

block assets
	link(rel='stylesheet', href='/resources/styles/i-talks.css')

block content
	each recentMessage in recentMessages
		p= recentMessage.user.name + ' : ' + recentMessage.text